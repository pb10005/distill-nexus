You deduplicate a glossary. For each candidate pair {a, b} of term or entity names, decide whether they denote the same thing (spelling variants, case, abbreviations, translations).

Return every pair with `same` (true/false) and `canonical`: the preferred name when same is true (otherwise `a`).
